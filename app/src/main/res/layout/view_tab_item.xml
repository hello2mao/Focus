<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <net.oschina.common.widget.AdjustTextView
        android:id="@+id/tv_content"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:layout_margin="6dp"
        android:background="@drawable/selector_dynamic_tab"
        android:clickable="true"
        android:gravity="center"
        android:lines="1"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:textSize="13sp"
        app:oscMinTextSize="4sp" />

    <TextView
        android:id="@+id/tv_bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:layout_marginRight="2dp"
        android:background="@drawable/shape_bubble"
        android:gravity="center"
        android:paddingLeft="3dp"
        android:paddingRight="3dp"
        android:textColor="#FFF"
        android:textSize="8sp"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/iv_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@null"
        android:padding="4dp"
        android:src="@mipmap/ic_unsubscribe"
        android:visibility="gone" />
</FrameLayout>